import Base from './base';
export default class Transform extends Base {
    transform(name: string, resource: any): {};
    private transformService;
    private transformFunction;
    private transformTrigger;
}
